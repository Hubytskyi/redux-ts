import React from 'react';
import { Box, Typography } from '@mui/material';

const Hero = () => {
  return (
    <Box sx={{ mt: 20 }}>
      <Typography variant={'h1'}>TypeScript, RTK Query, MUI, React testing library!</Typography>
    </Box>
  );
};

export default Hero;
